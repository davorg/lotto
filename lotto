#!/usr/bin/env perl

use strict;
use warnings;
use 5.010;

use FindBin '$Bin';
use lib $Bin;
use Lotto;

my ($type, $count) = parse_args(@ARGV);

for (1 .. $count) {
  my @nums = lotto($type);
  local $" = ', ';
  say join ' : ', map { "@$_" } @nums;
}
