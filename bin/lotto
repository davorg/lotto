#!/usr/bin/env perl

use strict;
use warnings;
use 5.010;

use FindBin '$Bin';
use lib "$Bin/../lib;
use Lotto;

my $lotto = Lotto->new(@ARGV);

for my $line ($lotto->play) {
  local $" = ', ';
  say join ' : ', map { "@$_" } @$line;
}
